define([ 'director' ], function (director) {
  var currentNav = null;

  var someFunction = function() {
    // Text
  };

  'use strict';
  return {
      github        :  github.init
    , blog          :  blog.init
    , about         :  about.init
  };
});
