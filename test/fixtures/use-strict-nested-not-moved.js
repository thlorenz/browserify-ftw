define([ 'director' ], function (director) {
  var currentNav = null;

  var nested = function() {
    'use strict';

    // A comment
  };

  return {
      github        :  github.init
    , blog          :  blog.init
    , about         :  about.init
  };
});
